# Create virtual environment (use your installed Python version)
py -m venv .venv

# Activate it
.\.venv\Scripts\Activate.ps1

pip --version
pip install -r requirements.txt

python -m uvicorn main:app --reload